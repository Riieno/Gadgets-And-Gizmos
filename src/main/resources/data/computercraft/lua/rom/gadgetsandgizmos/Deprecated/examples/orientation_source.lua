-- Send a temporary orientation target then clear it again

--[[--------------------------------------------------------##---------------------------------------------------------

=======================================================================================================================
                                                           MAIN
=======================================================================================================================

------------------------------------------------------------##-----------------------------------------------------]]--

local source = assert(peripheral.find("virtual_orientation_source"),
    "Attach a virtual orientation source")

source.setAnglesDegrees(10, -15)
sleep(2)
textutils.pretty_print(source.getState())
source.clear()
