-- Basic wheel mount drive test | Restores normal control when finished

--[[--------------------------------------------------------##---------------------------------------------------------

=======================================================================================================================
                                                           MAIN
=======================================================================================================================

------------------------------------------------------------##-----------------------------------------------------]]--

local wheel = assert(peripheral.find("wheel_mount"), "Attach an Offroad wheel mount")

wheel.setControls(0.6, 0, 0)
sleep(1)
textutils.pretty_print(wheel.getStatus())
wheel.clearControls()
